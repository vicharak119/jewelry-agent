{
  "name": "jewelry-agent-cloudflare",
  "version": "7.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev public --d1=DB --r2=STORAGE",
    "deploy": "npx wrangler pages deploy public --project-name=jewelry-agent"
  },
  "dependencies": {
    "jsdom": "^29.1.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
